import React from 'react';
import { render } from '@testing-library/react';
import { WeatherWidget } from './weather.composition';

describe('WeatherWidget', () => {
  it('should render the weather widget correctly', () => {
    expect(true).toBe(true);
  });
})